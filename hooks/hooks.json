{
  "description": "ziptie — JIT rule delivery",
  "hooks": {
    "PreToolUse": [
      {
        "hooks": [
          { "type": "command", "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/pretooluse.py\"", "timeout": 10 }
        ]
      }
    ]
  }
}
