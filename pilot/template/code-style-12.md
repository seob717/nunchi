# 코드 스타일 규칙

이 저장소의 모든 코드 변경은 아래 규칙을 반드시 지킨다.

1. 새로 작성하는 함수에는 JSDoc 주석을 단다 (`@param`, `@returns` 포함).
2. 임포트 순서: Node 내장 모듈 → 외부 패키지 → 내부 모듈. 그룹 사이는 빈 줄.
3. `console.log`를 커밋에 남기지 않는다. 로깅은 `src/logger`를 통해서만 한다.
4. `any` 타입을 사용하지 않는다.
