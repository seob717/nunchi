# strength 판정 지침 보강 — 결과 (2026-07-12, 이슈 #15)

사전등록: `DESIGN-strength-guidance.md`(f95ee7e). 처치 = `commands/compile.md` §4를
화행 기반 판정 + 언어별 보정 예시로 개정(같은 커밋). before = #13 실행
(`RESULTS-compile-multilingual.md`, 6e5110e), after = 동일 15셀·동일 프로토콜 재실행
(번역본 동결 SHA 재검증, 컴파일 sonnet 셀당 1개, 매칭 Codex 새 세션 11셀).

**한 줄 요약**: M5 strength 일치가 전체 79/129(61%) → 108/132(82%)로 올랐고,
지배적 결함이던 block→require-read 하향이 38건 → 7건으로 줄었다. 게이트
(recall·over-extraction·형식) 전부 통과 → **§4 개정 유지**. 비용: 과강화
(require-read/inject→block) 3건이 새로 생겼다(v1 0건).

## 0. 실행 기록·프로토콜 이탈

- supabase.ko는 #13의 번역 실패 셀을 그대로 유지(사전등록) — 양쪽 비교에서 제외.
- 중복 판정 세션 생략(사전등록) — #13 실측 드리프트 0%에 의존.
- 컴파일 출력 처리: 마크다운 펜스 감싼 셀 다수 + JSON 앞에 해설 문장을 붙인 셀
  3건(nextjs.en/ko, microfolio.es) — 내용 스키마는 전부 유효해 기계적으로 제거 후
  채택(공식 재시도 0회). 전송 계층 HTML 이스케이프는 저장 시 복원. 저장 시 전 룰
  정규식 컴파일·어휘 검증 통과.
- 매칭 11세션 전부 정상 종료·무결성 검사 통과(gold 전수 판정·1:1·gold 동결).

## 1. 게이트 판정 (사전등록 §3)

| 게이트 | 기준 | 결과 | 판정 |
|---|---|---|---|
| M1 recall | 셀별 3룰 초과 하락 없음 | 최대 하락 −2 (nextjs.ko 26→24) | **통과** |
| M4 over-extraction | 전 셀 0 유지 | 11셀 전부 0 | **통과** |
| M2 형식 | 위반 0 유지 | 15셀 전부 0/룰 | **통과** |

recall 상세(v1→v2): langgraph 전 언어 2→2, supabase en 5→5 · ja 5→7 · es 6→5,
nextjs en 26→27 · ko 26→24 · ja 24→26 · es 29→30. 변동 폭(−2~+2)은 #13에서 관측한
런간·언어간 변동 범위(±3) 안이다.

## 2. M5 — strength 일치 전후 (주지표)

| cell | v1 일치 | v2 일치 | block→하향 v1→v2 |
|---|---|---|---|
| langgraph en/ko/ja/es | 1/2 · 1/2 · 2/2 · 1/2 | **2/2 전 언어** | 3 → 0 |
| supabase en/ja/es | 3/5 · 2/5 · 4/6 | 3/5 · 5/7 · 4/5 | 3 → 0 |
| nextjs en/ko/ja/es | 16/26 · 17/26 · 15/24 · 17/29 | 22/27 · 19/24 · 22/26 · 25/30 | 32 → 7 |
| **전체** | **79/129 (61%)** | **108/132 (82%)** | **38 → 7** |

- **전 셀에서 개선 또는 동률**(악화 셀 0). 문서당 1런이라 통계적 주장은 하지
  않지만, 개선 방향이 12셀 중 11셀에서 일관되고 크기(+21%p)가 #13에서 실측한
  판정 노이즈(드리프트 0%)·recall 변동(±3룰)보다 훨씬 크다.
- **언어 축 목표 달성**: v1에서 ja만 맞히던 langgraph double-backtick 룰
  ("Do NOT" → "絶対に")을 v2는 en/ko/ja/es 전부 block으로 판정해 gold와 일치.
  ko "…하지 마세요", es "NO uses"가 보정 예시대로 금지로 읽혔다.
- **권고 예시도 작동**: supabase "avoid barrel"을 ja("避けてください")와
  es("Evita")가 inject로 판정 — v1의 require-read 획일화 대비 화행을 구분했다.
- **새 비용 — 과강화 3건**(v1 0건): nextjs.en require-read→block 1,
  nextjs.ko require-read→block 1 + inject→block 1. 하향 31건 감소와 교환된
  비용으로, 순효과는 명확히 양(+)이나 block 남발의 싹이라 후속 관찰 대상.

## 3. M3·microfolio (게이트 아님, 참고)

- 사전 고정 정탐 ②(브랜치 전환 후 재빌드 × `git checkout -b feat/x`)는 이번에도
  전 4언어 발동 — 정탐 제외. 그 외 위반: en 3 · ko 2 · ja 3 · es 1
  (v1: 1·2·2·5). v1에서 ko/ja/es에만 나타나던 `\b(gh|curl)\b`형 광역 트리거가
  이번엔 en에서 나왔고 es는 1건으로 줄었다 — 언어 효과가 아니라 런간 비결정성이라는
  #13 §4의 해석을 강화한다. highlight 룰의 `\.(md|mdx)$` 광역 path로 README.md까지
  잡은 것이 증가분의 대부분.
- microfolio(기대 0룰): en 0 · ko 0 · ja 1(Ak 접두 관례 승격) · es 0
  (v1: 1·0·1·3). 합계 4→1로 줄었지만 처치와 인과를 주장하지 않는다(비결정성).

## 4. evidence 대조

매치 룰 중 정확 부분문자열 미일치 43건 → 공백·마크다운 장식 정규화 후 14건 →
"…" 분절 대조 후 9건 → 전수 원문 grep 판독 결과 **전부 실재, 날조 0**.
유형은 v1과 동일: 백틱 래퍼 공백 차이(` ``code`` `), 줄 경계·코드펜스 이어붙임
(`git checkout <branch>`), 인접 절 결합("ユーザーから" + 다음 줄).

## 5. 해석·한계

- 이슈 #15의 가설 — "strength 불일치의 지배 패턴(block 하향)은 §4 지침이 원인이고,
  화행 원칙 + 언어별 예시로 고칠 수 있다" — 가 이 파일럿 조건에서 지지됐다.
  recall·과추출·형식은 건드리지 않고 M5만 움직였다.
- 한계: 문서당 1런(런간 분산과 처치 효과 완전 분리 불가), 번역 코퍼스(야생 비영어
  문서보다 쉬운 조건), 판정자 GPT 계열 1종, 중복 판정 생략(#13 실측 0%에 의존).
- 후속: 과강화 3건 추이 관찰, 야생 비영어 CLAUDE.md 표집(#15 잔여 항목),
  한국어 CLAUDE.md 작성 가이드 소재화(#15 별도 트랙).
